---
title: "Statystyka Laboratorium 1"
author: "Micha£‚ Aleksandrowicz 99005"
date: "15 marca 2019"
output:
  html_document: default
  pdf_document: default
---

By zacz¹æ pracê na laboratorium 1 potrzebne nam s¹ odpowiednie sk³adniki.
<br>Pobieramy je zatem oraz uruchamiamy.

```
install.packages("dplyr")
install.packages("devtools")
devtools::install_github("pbiecek/PogromcyDanych")
library(dplyr)
library(PogromcyDanych)
```

1. Która Marka wystêpuje najczêœciej w zbiorze danych auta2012?

Na samym pocz¹tku musimy zaznaczyæ ¿e chcemy u¿ywaæ zbioru auta2012.
<br>Dodatkowo do dalszych operacji bêdziemy potrzebowali przekazaæ informacje dalej.
<br>U¿yjemy do tego specjalnego znacznika.

```
auta2012%>%
  group_by(Marka)%>%
  summarise(liczba = n())%>%
  arrange(desc(liczba))%>%
  filter(liczba==max(liczba))

```

2.Spoœród aut marki Toyota, który model wystêpuje najczêœciej?

W tym przyk³adzie u¿yjemy funkcji filter. Wyszuka ona nam tylko te dane które jej wska¿emy.
<br>W naszym przypadku bêd¹ to auta marki Toyota.

```
auta2012%>%
  filter(Marka=="Toyota")%>%
  group_by(Model)%>%
  summarise(liczba = n())%>%
  arrange(desc(liczba))%>%
  filter(liczba==max(liczba))
```

3.SprawdŸ ile jest aut z silnikiem diesla wyprodukowanych w 2007 roku?

```
auta2012%>%
  select(Rodzaj.paliwa, Rok.produkcji)%>%
  filter(Rodzaj.paliwa=="olej napedowy (diesel)", Rok.produkcji == "2007")%>%
  summarise(liczba = n())%>%
  arrange(desc(liczba)) 
```

3b.Jakiego koloru auta maj¹ najmniejszy medianowy przebieg?

```
auta2012%>%
  select(Kolor, Przebieg.w.km)%>%
  group_by(Kolor, Przebieg.w.km)%>%
  summarise(mediana = median(Przebieg.w.km, na.rm = TRUE))%>%
arrange(mediana)
```

4.Gdy ograniczyæ siê tylko do aut wyprodukowanych w 2007, która Marka wystêpuje najczêœciej w zbiorze danych auta2012?

```
auta2012%>%
  select(Rok.produkcji, Marka)%>%
  group_by(Rok.produkcji, Marka)%>%
  filter(Rok.produkcji==2007)%>%
  summarise(liczba = n())%>%
  arrange(desc(liczba))%>%
  filter(liczba==max(liczba))
```

5.Spoœród aut marki Toyota, który model najbardziej straci³ na cenie pomiêdzy rokiem produkcji 2007 a 2008.

W tym zadaniu u¿yjemy kilku nowych funkcji:
<br>-mutate: S³u¿y do dodawania nowych kolumn
<br>-lag: opóŸnia nam ona wartoœæ funkcji o okreœlon¹ liczbê czasu.
<br>-na.omit: usuwa wszystkie niekompletne przypadki obiektu danych (zwykle ramki danych, macierzy lub wektora).

```
auta2012%>%
  select(Marka, Rok.produkcji, Model, Cena.w.PLN)%>%
  filter(Marka=="Toyota",Rok.produkcji>=2007, Rok.produkcji <=2008)%>%
  group_by(Model, Rok.produkcji)%>%
  summarise(Cena.w.PLN=mean(Cena.w.PLN), na.rm = TRUE)%>%
  mutate(roznica=Cena.w.PLN-lag(Cena.w.PLN,1))%>%
  select(Rok.produkcji, Model, roznica)%>%
  na.omit%>%
  arrange(desc(roznica))%>%
  filter(roznica==max(roznica))
```

6.Spoœród aut z silnikiem diesla wyprodukowanych w 2007 roku która marka jest najdro¿sza?

```
auta2012%>%
  select(Marka, Model, Cena.w.PLN, Rodzaj.paliwa, Rok.produkcji)%>%
  filter(Rok.produkcji==2007, Rodzaj.paliwa=="olej napedowy (diesel)")%>%
  group_by(Marka, Model, Cena.w.PLN)%>%
  arrange(desc(Cena.w.PLN))
```
  
7.Ile jest aut z klimatyzacj¹?

```
auta2012%>%
  select(Wyposazenie.dodatkowe)%>%
  filter(Wyposazenie.dodatkowe=="klimatyzacja")%>%
  summarise(liczba = n())%>%
  arrange(desc(liczba))
```
  
8.Gdy ograniczyæ siê tylko do aut z silnikiem ponad 100 KM, która Marka wystêpuje najczêœciej w zbiorze danych auta2012?

```
auta2012%>%
  select(KM,Marka)%>%
  filter(KM>100)%>%
  group_by(Marka)%>%
  summarise(liczba = n())%>%
  arrange(desc(liczba))
```

Niestety z braku rozwi¹zania jestem zmuszony do pominiêcia zadania 9.

10.Spoœród aut z silnikiem diesla wyprodukowanych w 2007 roku która marka jest najtañsza?

```
auta2012%>%
  select(Marka, Model, Cena.w.PLN, Rodzaj.paliwa, Rok.produkcji)%>%
  filter(Rok.produkcji==2007, Rodzaj.paliwa=="olej napedowy (diesel)")%>%
  group_by(Marka, Model, Cena.w.PLN)%>%
  arrange(Cena.w.PLN)
```

11.W jakiej marce klimatyzacja jest najczêœciej obecna?

```
auta2012%>%
  select(Marka, Wyposazenie.dodatkowe)%>%
  filter(Wyposazenie.dodatkowe=="klimatyzacja")%>%
  group_by(Marka)%>%
  summarise(liczba = n())%>%
  filter(liczba==max(liczba))
```

12.Gdy ograniczyæ siê tylko do aut o cenie ponad 50 000 PLN, która Marka wystêpuje najczêœciej w zbiorze danych auta2012?

```
auta2012%>%
  select(Cena.w.PLN, Marka)%>%
  group_by(Cena.w.PLN, Marka)%>%
  filter(Cena.w.PLN>50000)%>%
  group_by(Marka)%>%
  summarise(liczba = n())%>%
  arrange(desc(liczba))%>%
  filter(liczba==max(liczba))
```
13.Spoœród aut marki Toyota, który model ma najwiêkszy medianowy przebieg?

```
auta2012 %>%
  filter(Marka == "Toyota") %>%
  group_by(Model, Rodzaj.paliwa) %>%
  summarise(Mediana_przebiegu = median(Przebieg.w.km, na.rm=TRUE))%>%
  arrange(desc(Mediana_przebiegu))
```

14.Spoœród aut z silnikiem diesla wyprodukowanych w 2007 roku który model jest najdro¿szy?

```
auta2012%>%
  filter(Rok.produkcji==2007, Rodzaj.paliwa=="olej napedowy (diesel)")%>%
  group_by(Marka,Model)%>%
  summarise(liczba = max(Cena.w.PLN))%>%
  arrange(desc(liczba))
```

15.W jakim modelu klimatyzacja jest najczêœciej obecna?

```
auta2012%>%
  select(Model, Wyposazenie.dodatkowe)%>%
  filter(Wyposazenie.dodatkowe=="klimatyzacja")%>%
  group_by(Model)%>%
  summarise(liczba = n())%>%
  filter(liczba==max(liczba))
```

16.Gdy ograniczyæ siê tylko do aut o przebiegu poni¿ej 50 000 km o silniku diesla, która Marka wystêpuje najczêœciej w zbiorze danych auta2012?

```
auta2012%>%
  select(Przebieg.w.km, Marka,Rodzaj.paliwa)%>%
  filter(Przebieg.w.km<50000,Rodzaj.paliwa=="olej napedowy (diesel)")%>%
  group_by(Marka)%>%
  summarise(liczba = n())%>%
  filter(liczba==max(liczba))
```

17.Spoœród aut marki Toyota wyprodukowanych w 2007 roku, który model jest œrednio najdro¿szy?

```
auta2012%>%
  filter(Rok.produkcji==2007, Marka=="Toyota")%>%
  group_by(Model)%>%
  summarise(Srednia_cena = mean(Cena.w.PLN, na.rm=TRUE))%>%
  arrange(desc(Srednia_cena))
```

18.Spoœród aut z silnikiem diesla wyprodukowanych w 2007 roku który model jest najtañszy?

```
auta2012%>%
  select(Model, Cena.w.PLN, Rodzaj.paliwa, Rok.produkcji)%>%
  filter(Rok.produkcji==2007, Rodzaj.paliwa=="olej napedowy (diesel)")%>%
  group_by(Model)%>%
  arrange(Cena.w.PLN)
```

19.Jakiego koloru auta maj¹ najwiêkszy medianowy przebieg?

```
auta2012%>%
  select(Kolor,Przebieg.w.km)%>%
  group_by(Kolor)%>%
  summarise(Przebiegg = median(Przebieg.w.km, na.rm=TRUE))%>%
  arrange(desc(Przebiegg))
```
